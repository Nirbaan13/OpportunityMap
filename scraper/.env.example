# Database connection (same as backend for direct writes)
DATABASE_URL=postgresql+psycopg://opportunitymap:opportunitymap_dev@localhost:5432/opportunitymap

# Optional: use backend API instead of direct DB writes (Phase 2+)
# API_URL=http://localhost:8000
# API_KEY=
